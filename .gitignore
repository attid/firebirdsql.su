# Python
__pycache__/
*.pyc

# Node / Astro
node_modules/
site/dist/
site/.astro/

# Карта кодовой базы (скилл codespaces) — генерируется, не версионится
.belief_map.sexp
belief_map.sexp

# Мусор ОС
.DS_Store
Thumbs.db

# Секреты деплоя
deploy/.env

# Метрики базлайна (Метрика/GSC выгрузки) — только локально, в публичный
# репозиторий не попадают: трафик по страницам не для чужих глаз
docs/metrics-baseline/

# Исходники Language Reference (клонируются tools/parse_langref.py, см. docstring)
docs/langref-src/
