---
title: "DROP TABLE"
old_id: drop_table
section: glossary
type: term
firebird:
  since: "0.9"
  until: 
  deprecated: false
---

# DROP TABLE

## Версии сервера
| 0.9 | 1.0 | 1.5.3 | 1.5.4 | 1.5.5 | 2.0 | 2.0.3 | 2.0.4 | 2.1 | 2.5 | 3.0 |
|---|---|---|---|---|---|---|---|---|---|---|
| Да | Да | Да | Да | Да | Да | Да | Да | Да | Да | Да |

## Формат
```
DROP TABLE name;
```

| Аргумент | Описание |
|---|---|
| name | Имя существующей таблицы. |

## Описание
DROP TABLE удаляет данные, метаданные и индексы таблицы из базы данных. Эта инструкция так же удаляет любые триггеры, которые обращаются к таблице.

Таблица, на которую ссылаются выражения SQL, в образы, ограничения целостности или сохраненные процедуры, не может быть удалена. Таблица, используемая в активных транзакциях, не может быть удалена, пока ее не освободят.

⚠️ Когда удаляется внешняя таблица, DROP TABLE только удаляет определение таблицы из базы данных. Внешний файл не будет удален.

Таблица может быть удалена ее создателем и пользователем SYSDBA.

## Пример
Следующая инструкция удаляет таблицу с названием "COUNTRY".
```
DROP TABLE COUNTRY;
```

## См. также
[CREATE TABLE](/create_table/),  [ALTER TABLE](/alter_table/),  [CREATE VIEW](/create_view/),  ALTER VIEW

## Источник
langref.pdf
